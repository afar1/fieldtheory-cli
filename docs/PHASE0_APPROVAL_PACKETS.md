# Field Theory Phase 0 Approval Packets

Status: checkpoint from July 2, 2026. These packets translate the Phase 0 runbook into approval scopes. They do not grant approval by themselves.

Source artifacts:

- Source plan: `/Users/afar/.fieldtheory/library/Plans/Field Theory Ecosystem Improvement Technical Plan.md`
- Execution ledger: `docs/ECOSYSTEM_EXECUTION_STATUS.md`
- Preservation runbook: `docs/PRESERVATION_GATES.md`
- Read-only preflight checklist: `docs/PHASE0_PREFLIGHT_CHECKLIST.md`

## Approval Rules

Approval must use the exact listed approval phrase for the packet. Nearby wording, summaries, "yes to A5a"-style shorthand, or this file's existence do not count as approval. A packet approval does not imply approval for adjacent work.

Run the matching read-only preflight checklist in the same execution shell immediately before asking for or using an approval, except for user-paused A4: do not run A4 preflights or ask for A4 approval until Andrew explicitly resumes A4 after connecting the backup disk. If current evidence contradicts the packet assumptions, stop and patch the runbook instead of asking for or executing the packet.

Default denied operations:

- clone operations;
- builds or project generation;
- public pushes;
- private pushes outside the named packet;
- deletes, prunes, or stashes;
- installs;
- backups or restores;
- manifests, bundles, archives, or snapshots;
- plugin cache mutation;
- release tags or release publication;
- backup scheduling or LaunchAgent loading, unless the packet explicitly says so.

## Packet A1: Private iOS Preservation Push And Verification

Approval phrase: `approve A1 private push and verification 60e7e9ddbc2f98a0233d8769539b1fb4b189214a 56fab3bf8192826fa9558392927b27cada070af1`.

Goal: preserve the stranded native iOS app on the private `afar1/fieldtheory-labs` remote without touching the public `afar1/fieldtheory` remote.

Approved object IDs:

- archive branch object: `60e7e9ddbc2f98a0233d8769539b1fb4b189214a`;
- original iOS branch object: `56fab3bf8192826fa9558392927b27cada070af1`.

Allowed operations:

- re-run A1 read-only preflight checks, including public-origin exact-ref and `origin/main` tree guards, in the same execution shell immediately before use;
- verify the two local branch heads still equal the approved object IDs above;
- push only these two exact objects to the `labs` remote:
  - `60e7e9ddbc2f98a0233d8769539b1fb4b189214a` to `refs/heads/codex/archive-ios-native-20260614`;
  - `56fab3bf8192826fa9558392927b27cada070af1` to `refs/heads/ios-native-app`;
- fetch those two private branch refs into local `refs/remotes/labs/...` refs and verify they resolve to the approved object IDs;
- run private Git verification commands non-interactively with `GIT_TERMINAL_PROMPT=0`, including `ls-remote`, `fetch`, and `git clone`; if any private Git verification command fails, stop and record A1 as partial or blocked;
- set local upstream tracking to the private remote for those two local branches;
- create a fresh temporary private clone of the archive branch;
- verify the fresh clone contains `ios-native/`;
- generate the iOS Xcode project and attempt the README-backed iOS build from the fresh clone:
  - from the fresh clone root, run `./ios-native/scripts/build-whisper-ios.sh`;
  - from `ios-native`, run `xcodegen generate`;
  - from `ios-native`, run `xcodebuild -project FieldTheory.xcodeproj -scheme FieldTheory -configuration Debug -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build`;
- record the exact build blocker if the fresh clone cannot build.

Explicitly not allowed:

- any push to `origin` in `/Users/afar/dev/fieldtheory`;
- any public repo push;
- force push;
- deletion of `/Users/afar/dev/fieldtheory-labs-ios-native-archive`;
- extraction into a new iOS repo;
- TestFlight, fastlane, or signing work.

Build verification caveats:

- the fresh clone does not contain `build-apple/whisper.xcframework`; it is generated by `./ios-native/scripts/build-whisper-ios.sh` and ignored by git;
- `./ios-native/scripts/build-whisper-ios.sh` deletes and recreates `build-apple`, `build-ios-sim`, and `build-ios-device` inside the fresh clone;
- `xcodegen generate` writes `ios-native/FieldTheory.xcodeproj`;
- the build command targets simulator/no-code-sign verification only; signing, provisioning, device, TestFlight, or distribution work requires a separate explicit approval packet.

Completion evidence:

- `afar1/fieldtheory-labs` reports `PRIVATE`;
- both required branches exist on the private remote and resolve to the approved object IDs;
- both fetched private remote-tracking refs under `refs/remotes/labs/...` resolve to the approved object IDs;
- both local branches report the private upstream in `git status --short --branch`;
- a fresh private clone contains `ios-native/`;
- the README-backed iOS build command succeeds from the fresh clone. If the fresh private clone does not build, the exact build blocker must be recorded and A1 remains partial or blocked unless Andrew says the exact phrase `waive A1 fresh-clone build gate after recorded blocker`.

If the fresh private clone does not build, A1 is only partially executed or blocked. A recorded build blocker does not complete A1 or Phase 0 unless Andrew says the exact phrase `waive A1 fresh-clone build gate after recorded blocker`. Without that exact waiver phrase, do not call A1 complete and do not use A1 to unblock E1/E2 or Phase 1+.

## Packet A4a: Manual Waist Backup And Restore Drill

Approval phrase: `approve A4a manual backup and restore drill`.

Goal: create the encrypted restic backup repository, take a manual backup of both Field Theory local data roots, and prove restore from a captured snapshot ID.

Current status: user-paused and not askable yet. Andrew paused backup work until he connects a backup disk and explicitly resumes A4. During the pause, do not ask for A4 approval, run A4 preflights, install backup tools, initialize repositories, back up, restore, or schedule backup work. The July 2 09:48Z direct preflight, Pascal the 2nd's independent check, and Goodall the 2nd's adversarial review show `/Volumes/Extreme SSD` is absent, not mounted, not writable, and not known to `diskutil info`. `restic` is also missing from `PATH`; the generic A4a approval phrase does not allow installing it unless the one-time approval text explicitly names installing `restic` after the mounted-volume preflight passes. Time Machine knows an `Extreme SSD` destination, but the approved backup volume is not currently mounted for this packet. Backup-looking local folders still do not prove A4 restore safety. The packet's backup scope must cover both `~/.fieldtheory` and the real bookmark store at `~/.ft-bookmarks`, not only the symlink path.

Precondition:

- Andrew has explicitly resumed A4 after connecting the backup disk;
- the approved backup volume is mounted, writable, and verified by mount/disk metadata, currently expected as `/Volumes/Extreme SSD`.

Allowed operations:

- rerun the A4a mounted-volume/restic read-only preflight in the same execution shell immediately before use and stop if approved-volume evidence no longer matches;
- install `restic` only if the exact one-time approval text explicitly names installing `restic`; otherwise stop if `restic` is missing;
- create local restic config/password files with private file modes;
- initialize the approved restic repository if needed;
- run one manual backup covering:
  - `~/.fieldtheory`;
  - the real bookmark store currently reached through `~/.fieldtheory/bookmarks`, expected as `~/.ft-bookmarks`;
- exclude only documented rebuildable indexes and caches;
- create one drill file in each root;
- capture the backup snapshot ID;
- delete only the two drill files;
- restore from the captured snapshot ID into a unique `mktemp -d /tmp/fieldtheory-restore-drill.XXXXXX` directory;
- verify both restored hashes;
- copy the drill files back;
- write the restore drill artifact.

Explicitly not allowed:

- installing or loading a LaunchAgent, cron entry, or other scheduled job;
- deleting or moving bookmark/library data;
- changing symlinks;
- running B3/B4/F1 migrations;
- deleting cache/index files outside the two drill files.

Completion evidence:

- restic repo path and password-file path are recorded;
- restic snapshot ID is recorded;
- the snapshot includes both roots;
- restore drill artifact records repo path, roots, snapshot ID, and both hashes;
- both drill files are restored in their original locations.

This packet does not complete A4 by itself. A4 also requires scheduling unless Andrew says the exact phrase `waive A4 scheduled-backup gate after manual backup and restore drill`. Without that exact waiver phrase, do not call A4 complete and do not use A4 to unblock B3/B4/F1 or Phase 1+.

## Packet A4b: Scheduled Waist Backup And Scheduled Restore Drill

Approval phrase: `approve A4b scheduled backup and restore drill`.

Goal: install and observe a scheduled encrypted backup job after A4a has passed.

Preconditions:

- A4a manual backup and restore drill passed;
- the exact script, plist or scheduling mechanism, log path, restic repo, retention policy, and restore-drill artifact format have been reviewed.

Allowed operations:

- rerun the A4b scheduled-backup read-only preflight in the same execution shell immediately before use and stop if A4a evidence, reviewed scheduler paths, repo, retention policy, or artifact format no longer match;
- create the approved backup script;
- create the approved LaunchAgent, cron entry, or equivalent scheduler config;
- load or enable that scheduler;
- trigger or wait for one scheduled run;
- inspect logs and restic snapshot evidence for that scheduled run.

Explicitly not allowed:

- changing backup roots without a new review;
- deleting old snapshots through a retention policy unless the policy is explicitly approved;
- running migrations after the scheduled run unless separately approved.

Completion evidence:

- scheduler config exists at the reviewed path;
- one scheduled run is observed;
- logs show success;
- restic contains a new snapshot from the scheduled job;
- scheduled-run snapshot ID is recorded;
- a restore drill from that exact scheduled-run snapshot recovers one file from each root with matching hashes.

## Packet A2a: Create Mac App Preservation Bundle And Dirty-Tree Snapshot

Approval phrase: `approve A2a create mac-app preservation bundle and dirty-tree snapshot`.

Goal: preserve the mac app release branch's committed refs, dirty tracked edits, staged edits, and Git-visible untracked files before any split, stash, merge, tag, or public push. Ignored files are intentionally excluded from the untracked archive.

Current status: eligible to ask Andrew for approval only as preservation after the July 2 12:48Z dirty-set regeneration; not approved yet. The July 2 12:48Z direct read-only preflight shows the repo is public, the branch is dirty, package metadata says `0.3.20`, release metadata is stale, no source-repo tags were observed, and the live release feed is still `0.3.14`. The dirty-set proof now records 39 dirty paths: 28 modified tracked paths, 11 Git-visible untracked paths, and SHA-256 `0c9a09a13ed2583ea26f44dffeef44ae42990bdda929265f4f7501696cc4112d` for `git status --porcelain --untracked-files=all`. The two added Git-visible untracked paths since the prior tuple are `docs/plans/2026-07-02-001-feat-ios-live-dictation-plan.md` and `docs/plans/2026-07-02-002-feat-team-share-links-plan.md`. A dirty path count alone is not enough to prove the packet still matches. Do not treat this packet as permission to publish, tag, push, clean up, or call A2 complete.

Allowed operations:

- rerun the A2a read-only preflight in the same execution shell immediately before use, verify the dirty set still has 39 paths, 28 modified tracked paths, 11 Git-visible untracked paths, includes `docs/plans/2026-07-02-001-feat-ios-live-dictation-plan.md` and `docs/plans/2026-07-02-002-feat-team-share-links-plan.md`, and has hash `0c9a09a13ed2583ea26f44dffeef44ae42990bdda929265f4f7501696cc4112d`; stop if any part does not match;
- create the A2 git bundle inside a timestamped Desktop snapshot folder without overwriting an existing snapshot folder or bundle;
- verify the A2 git bundle;
- write status, tracked diff, staged diff, Git-visible untracked list, and Git-visible untracked archive to the Desktop snapshot folder;
- list the archive to prove it is readable.

Explicitly not allowed:

- stash;
- commit;
- switch branches;
- tag;
- push;
- publish release artifacts;
- deleting, moving, pruning, or cleaning files;
- builds, installs, backups, restores, scheduler changes, migrations, plugin-cache mutation, or A2 completion.

Completion evidence:

- timestamped snapshot folder and bundle file exist;
- bundle verification output exists;
- working-tree snapshot folder exists;
- status, tracked diff, staged diff, Git-visible untracked list, `untracked.tgz`, and `tar -tzf "$SNAPSHOT/untracked.tgz"` archive listing output exist.

This completes only the A2 preservation bundle and dirty-tree snapshot. A2 remains incomplete until the release train is clean, version/changelog/checklist state is aligned, release verification passes, public exposure is reviewed, release refs are pushed/tagged, and updater evidence resolves.

## Packet A2b: Public Release Review Artifact

Approval phrase: `approve A2b public release review only`.

Goal: after A2a preservation is complete and the release train has been made clean and internally consistent, create the public-release review artifact without publishing anything.

Current status: not askable yet. A2a preservation has not completed, the working tree is dirty, changelog/checklist state is stale, and no clean `0.3.20` release candidate has been reviewed.

Allowed operations:

- rerun the A2b public-release review preflight in the same execution shell immediately before use and stop if public-repo, clean-tree, metadata, changelog, or checklist evidence no longer matches;
- verify the source repo is public;
- verify the working tree is clean;
- verify package metadata, changelog, and release checklist all target `0.3.20`;
- record reviewed HEAD, commit list, changed file list, and exposure-scan output in a timestamped Desktop review folder.

Explicitly not allowed:

- committing;
- switching branches;
- creating or moving tags;
- pushing;
- publishing release artifacts;
- changing release metadata.

Completion evidence:

- public-review artifact folder exists;
- reviewed HEAD SHA is recorded;
- commit list and changed file list exist;
- exposure-scan output exists and has a written disposition before any final push is considered.

This completes only the public-release review artifact. It does not approve commit preparation, tag creation, public push, release publication, or updater-feed changes.

## Packet A2c: Final Public Release Push

Approval phrase: `approve A2c final public release push <reviewed-sha> v0.3.20`.

Goal: push the exact reviewed `main` commit and `v0.3.20` tag after A2a and A2b are complete and release verification has passed.

Current status: not askable yet. A2a and A2b have not completed, `main` and `v0.3.20` do not currently identify a reviewed clean release candidate, and there is no green release-verification artifact.

Allowed operations:

- rerun the A2c final-public-push preflight in the same execution shell immediately before use and stop if reviewed SHA, exposure disposition, release verification, local `main`, or `refs/tags/v0.3.20` evidence no longer matches;
- verify the reviewed SHA in the approval phrase matches the public-review artifact;
- verify the exposure-scan disposition exists;
- verify release verification passed;
- verify local `main` and `refs/tags/v0.3.20` both point at the reviewed SHA;
- push only that reviewed SHA to public `main`;
- push only `refs/tags/v0.3.20` pointing at that reviewed SHA.

Explicitly not allowed:

- force pushing;
- pushing any branch other than `main`;
- pushing any tag other than `v0.3.20`;
- creating the tag as part of this packet;
- publishing release binaries or updater metadata unless separately approved;
- changing source files before the push.

Completion evidence:

- approved reviewed SHA is recorded;
- `origin/main` resolves to the reviewed SHA;
- `refs/tags/v0.3.20` exists on `origin` and resolves to the reviewed SHA;
- no other refs were pushed.

This completes only the final source/tag push. A2 remains incomplete until release artifacts and updater evidence are separately verified or waived by a later named exact waiver phrase; this packet defines no such waiver.

## Packet A3a: Plugin Installed-State Snapshot

Approval phrase: `approve A3a plugin installed-state snapshot`.

Goal: durably snapshot the currently installed plugin and Codex plugin cache before any dev-repo commit strategy or cache mutation.

Current status: eligible to ask Andrew for approval only as installed/cache preservation; not approved yet. The July 2 12:14Z direct read-only preflight shows installed source and Codex cache are byte-identical, but the dev bundle differs from installed source in `mcp-server/server.js`, `scripts/smoke-test.js`, and `skills/current-document/SKILL.md`; the plugin repo is on `main...origin/main`, dirty at `4d7b20ae50659743802a28528a09c1fd1501a799` with 7 tracked modified files, 12 Git-visible untracked files, and status SHA-256 `39c3d661ab278fa7c597f092f3f850738334315e78ce8d7f507435838158dc76`. Sibling `agents/openai.yaml` files are present in installed/cache state, so treat any untracked dev sibling files as follow-on dev work, not as permission to install, mutate cache, commit dev changes, rename directories, delete `/private/tmp/a3_dev_files.txt` or `/private/tmp/a3_installed_files.txt`, or call A3 complete.

Allowed operations:

- rerun the A3a read-only preflight in the same execution shell immediately before use and stop if installed/cache evidence or plugin dev-repo branch, upstream, HEAD, dirty status, untracked set, or status hash no longer matches;
- create a new timestamped Desktop snapshot directory that must not already exist;
- rsync installed plugin source into a timestamped Desktop snapshot;
- rsync Codex cache plugin source into the same snapshot;
- verify installed and cache copies are byte-identical;
- write SHA-256 checksums for the `installed/` and `cache/` snapshot copies.

Explicitly not allowed:

- running `install.sh`;
- deleting or rewriting Codex plugin cache;
- committing plugin repo changes;
- renaming the version-stamped plugin directory;
- deleting `/private/tmp/a3_dev_files.txt` or `/private/tmp/a3_installed_files.txt`;
- public or private pushes;
- migrations;
- backup or restore work;
- stashing;
- switching branches;
- tag creation or tag movement;
- publishing release artifacts;
- deleting, moving, pruning, or cleaning files outside the approved fresh snapshot destination;
- running future A3 commit, install, or cache-mutation work without a later named approval packet;
- calling A3 complete.

Completion evidence:

- installed/cache snapshot directories exist;
- `diff -qr` between snapshot copies is clean;
- checksum file exists;
- `SHA256SUMS` contains only `installed/` and `cache/` entries, with no self-entry for the checksum file.

This completes only the installed/cache snapshot. A3 remains incomplete until the deployed state is committed, version authority is single-sourced, release tagging is resolved, install behavior is separately approved, and the plugin repo is clean after install.

## Packet A5a: Cleanup Manifest Only

Approval phrase: `approve A5a cleanup manifest only`.

Goal: enumerate cleanup candidates and create a cleanup preservation manifest without deleting anything.

Current status: eligible to ask Andrew for approval only as manifest work after the July 2 12:14Z worktree-count regeneration; not approved yet. The July 2 12:14Z direct read-only preflight shows 59 Field Theory-family directories, 60 target-repo worktree records, 5 existing `/private/tmp/fieldtheory-cli-*` directories, 17 prunable `fieldtheory-cli` worktree records, and four target-sensitive directories: `oscar`, `old-field`, `oscar-pr-101-preserved`, and `littleai`. These targets need target-specific preservation decisions before any deletion. It also shows the Oscar mirror at `1454e11c9b0b47b30b4ef0b356f334d2d6b8bb28` is 12 commits behind live GitHub `origin/main` at `f81481ff9824ddb4fa252ed93933d9bc815dc930`, so stale local remote refs are not enough for preservation evidence. Do not treat this packet as permission to archive, bundle, create waivers, delete, prune, move `oscar`, or call A5 complete.

Allowed operations:

- rerun the A5a read-only preflight in the same execution shell immediately before use and stop if inventory evidence no longer matches;
- enumerate Field Theory family directories;
- enumerate `/private/tmp/fieldtheory-cli-*` candidates;
- record each candidate's current preservation status and missing evidence;
- write the cleanup preservation manifest with proposed future target-specific packets.

Explicitly not allowed:

- creating git bundles, directory archives, or waiver text;
- deleting files or directories;
- `git worktree prune`;
- moving `oscar`;
- stashing or committing changes;
- deleting `/Users/afar/dev/fieldtheory-labs-ios-native-archive`;
- cloning, building, generating projects, installing tools, backing up, restoring, scheduling jobs, pushing, tagging, publishing releases, running migrations, or mutating plugin cache.

Completion evidence:

- cleanup preservation manifest exists;
- every candidate enumerated by the fresh A5a preflight, including protected and non-deletion candidates, has a current evidence status and a future preservation decision path;
- no denied operation was performed: no git bundle, directory archive, waiver text, deletion, worktree prune, path move, stash, commit, clone, build, project generation, install, backup, restore, scheduler change, push, tag, release publication, migration, or plugin-cache mutation.

This completes only the cleanup manifest. A5 remains incomplete until deletion/prune work is separately approved and executed, `~/dev` family count is reduced to the plan target, and the worktree policy note exists.

## Packet A6a: Optional Review Debris Quarantine

Approval phrase: `approve A6a review-debris quarantine`.

Goal: quarantine the pre-existing zero-byte `twitter-bookmarks.db` review-debris candidate without deleting it.

Current status: eligible to ask Andrew for optional quarantine approval; not approved yet. The July 2 12:14Z direct read-only preflight shows `bookmarks.db?immutable=1` remains absent and `twitter-bookmarks.db` is a zero-byte regular file, not a symlink. Its `stat -f '%i %z %m %HT'` tuple is `70248019 0 1776019722 Regular File`. Do not treat this packet as permission to delete anything.

Allowed operations:

- re-run A6a read-only preflight checks in the same execution shell immediately before use;
- create a timestamped Desktop quarantine directory that must not already exist;
- compare the target file's fresh `stat -f '%i %z %m %HT'` tuple against the approved evidence immediately before moving it;
- move only `/Users/afar/.ft-bookmarks/twitter-bookmarks.db` into that quarantine directory.

Explicitly not allowed:

- deleting the file;
- touching any other bookmark database or media file;
- changing symlinks;
- running bookmark migrations;
- cloning, building, generating projects, installing tools, backing up, restoring, creating manifests, creating bundles, creating archives, creating snapshots, mutating plugin cache, tagging, publishing releases, scheduling jobs, running migrations, pruning worktrees, stashing, committing, pushing, or moving any path except the exact target file into the fresh quarantine directory.

Completion evidence:

- preflight proves `twitter-bookmarks.db` still has the approved `stat -f '%i %z %m %HT'` tuple `70248019 0 1776019722 Regular File` and is not a symlink;
- quarantine directory exists and was not reused;
- `twitter-bookmarks.db` exists inside the quarantine directory;
- the quarantined `twitter-bookmarks.db` has the same inode, size, and modified time observed immediately before the move, is still a regular file, and is not a symlink;
- `/Users/afar/.ft-bookmarks/twitter-bookmarks.db` is absent after the move.

This completes only the optional A6a quarantine. It does not complete A5, Phase 0, backup work, restore work, bookmark migration, or any broader cleanup.
