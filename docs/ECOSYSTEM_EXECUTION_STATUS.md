# Field Theory Ecosystem Execution Status

Status: checkpoint from July 2, 2026. This file is an execution ledger for the Field Theory Ecosystem Improvement Technical Plan. The source plan remains `/Users/afar/.fieldtheory/library/Plans/Field Theory Ecosystem Improvement Technical Plan.md`.

This ledger exists to keep orchestration honest: an item is not complete unless the current evidence proves its definition of done. Approval-only operations stay approval-only even when their command shape is documented.

## Current Orchestrator Goal

Preserve code and data first, then progress through the plan in dependency order with subagent review and Andrew's exact approval phrase before risky operations.

Current orchestrator constraints:

- Do not push to public or private remotes without Andrew's exact approval phrase.
- Do not clone, build, install, back up, restore, schedule jobs, delete files, prune worktrees, move protected directories, stash, tag, publish releases, run migrations, create bundles/snapshots/manifests, or mutate plugin cache without Andrew's exact approval phrase and the matching packet scope.
- Treat A1 and A4 as gates for later iOS extraction, waist migrations, bookmark migration, and sync work.
- A4 backup work is user-paused until Andrew explicitly resumes it after connecting a backup disk. During the pause, do not ask for A4 approval, run A4 preflights, install backup tools, initialize repositories, back up, restore, or schedule backup work.
- The source plan says A1 and A4 gate Phase 1+, while the full Phase 0 exit remains broader. Do not treat Phase 1 as unblocked until A1 and A4 pass. If any Phase 0 exit gap remains after A1 and A4 pass, the only acceptance path is Andrew saying the exact phrase `accept Phase 1 start with remaining Phase 0 gaps: <named gap IDs>`. Do not infer acceptance from summaries, GO reports, or nearby wording. A1 build and A4 scheduling have their own exact waiver phrases.
- Use subagents for independent evidence gathering and adversarial review, but keep final sequencing and approval boundaries in the orchestrator.
- Use `docs/ECOSYSTEM_ACT_GOALS.md` as the reusable act-level goal map for Workstreams A-G. It coordinates future subagent briefs; it does not approve or complete any act.

Current subagent contract:

- Evidence subagents inspect current state read-only and return leads, not proof.
- Adversarial subagents review proposed packet scope, stale assumptions, and unsafe language before execution.
- The orchestrator directly verifies command output, patches docs when evidence changes, and decides the next safe step.
- The current read-only preflight checklist lives in `docs/PHASE0_PREFLIGHT_CHECKLIST.md`.
- Live preflight observations are recorded in `docs/PHASE0_PREFLIGHT_RESULTS.md`.

Recent goal contracts:

- Orchestrator: keep Phase 0 preservation-first, maintain the approval queue, integrate subagent evidence, and avoid mutating operations without Andrew's exact approval phrase. Current verification condition: A1 can only move from askable to executed after Andrew says the exact SHA-pinned phrase and a fresh A1 read-only preflight rerun in the same execution shell still matches.
- Noether: independently verified the current A1 facts read-only and found A1 GO to ask as a candidate approval request only; no approval packet was executed.
- Maxwell: adversarially reviewed the current Phase 0 docs, returned NO-GO for broad Phase 0 approval, and identified source-plan authority, A4 preflight/install ordering, subagent-proof wording, and paste-ready mutating-block risks. After those patches, Maxwell rereviewed the changed boundaries and returned GO to ask A1 only; A1 remains request-only.
- Franklin: independently verified A2a/A3a read-only after the direct 06:21Z preflight and found both GO to ask only, not execute.
- Locke: independently verified A5a/A6a read-only after the direct 06:21Z preflight and found both GO to ask only, not execute; Oscar remains preservation-sensitive and live GitHub remote truth is required.
- Einstein: adversarially reviewed the 06:21Z non-A1 request queue, initially found source-plan/runbook wording blockers, then rereviewed the patches and returned GO for the changed areas.
- Parfit: independently verified A4a/A4b read-only at 06:36Z and found A4a NO-GO / not askable and A4b NO-GO / blocked; no material ledger drift.
- Ptolemy: independently verified A4a/A4b read-only at 06:38Z and found A4a NO-GO and A4b NO-GO; `ShipIt` is updater evidence, not backup scheduler evidence.
- Aristotle: adversarially reviewed the A4 documentation patch, found unsafe "preflight still matches" wording in two mutating A4 template guards, and returned NO-GO until patched.
- Hume: rereviewed the patched A4 guards and returned GO; all A4 mutating guards now require a fresh passing A4a preflight.
- Laplace: independently verified A1 read-only at 06:49Z; branch/API/public-leak facts still support asking, but Laplace hit a subagent-environment private Git credential-helper failure. The coordinator reran the exact private `git ls-remote` checks successfully, so the caution is recorded but not current coordinator-shell drift.
- McClintock: adversarially reviewed the 06:49Z A1 evidence refresh and returned GO to leave the docs as-is; A1 remains request-only and NO-GO to execute without Andrew's exact phrase plus fresh matching preflight.
- Euler: adversarially reviewed the current approval queue at 07:00Z and returned GO to ask Andrew for A1 first; A1 removes the highest-severity iOS existence risk, A4 is not askable, and A2a/A3a/A5a/A6a first would be less aligned.
- Mendel: independently discovered the README-backed A1 post-push iOS build verification path read-only; no `.xcodeproj` is checked in, XcodeGen generates it from `ios-native/project.yml`, and the build uses `build-whisper-ios.sh` plus `xcodebuild`.
- Chandrasekhar: adversarially reviewed the A1 build-verification wording and returned GO; the patch does not authorize clone/build/generation before approval and keeps build success required unless Andrew says `waive A1 fresh-clone build gate after recorded blocker`.
- Hegel the 2nd: adversarially audited the current Phase 0 docs for A1/A4 contradictions, stale build wording, and approval drift; returned GO with only a wording-hardening note to prefer execution-shell preflight language.
- Banach the 2nd: independently verified A1 read-only at 07:23Z and returned GO-to-ask only; exact private target refs remain absent, public `origin/main` still has zero `ios-native` paths, and no approval or execution occurred.
- Boyle the 2nd: independently verified A4 read-only at 07:24Z and returned NO-GO/not askable; `/Volumes/Extreme SSD` is absent, `restic` is missing, no scheduler evidence exists, and no restore-drill artifact exists.
- Kant the 2nd: adversarially reviewed the refreshed A1/A4 sequencing and returned GO to update the ledger and keep A1 as the only recommended next gate while A4 and Phase 1 remain blocked.
- Hypatia: extracted Phase 0 and Act requirements from the source plan and found no material drift in the current execution ledger.
- Singer: adversarially reviewed the SHA-pinned A1 next-step decision, required a post-patch A1 evidence-log entry, then rereviewed and found no remaining blockers.
- Faraday the 2nd: independently verified A1 read-only at 09:42Z and returned GO-to-ask-only; exact private target refs remain absent, pinned local SHAs still match, and current public `origin/main` still has zero `ios-native` paths.
- Kepler the 2nd: adversarially reviewed the A1 request boundary at 09:42Z and returned OK to report A1 as askable only, not approved, not executed, and not complete.
- Pascal the 2nd: independently verified A4 read-only at 09:48Z and returned not askable, with one backup-scope warning: operational A4 must include both `~/.fieldtheory` and the real bookmark store at `~/.ft-bookmarks`.
- Goodall the 2nd: adversarially reviewed the A4 boundary at 09:48Z and returned OK to report A4a as not askable and A4b as blocked.
- Raman the 2nd: independently verified A2a/A3a read-only at 09:56Z. A2a remains ASK-ONLY with matching dirty-set hash; A3a is DRIFTED relative to the source-plan deployed/dev byte-identity premise but still useful as installed/cache preservation because installed source and Codex cache remain byte-identical.
- Turing the 2nd: adversarially reviewed A2a/A3a at 09:56Z. A2a boundary was OK; A3a needed a stronger stop gate for plugin dev-repo branch, upstream, HEAD, dirty status, and untracked follow-on file set. After the patch, Turing returned OK to report progress.
- Sartre the 2nd: independently verified A5a/A6a read-only at 10:13Z. A5a remains ASK-ONLY with matching directory, worktree, tmp, Oscar, mirror, and protected-archive evidence. A6a remains ASK-ONLY with the same zero-byte regular-file tuple.
- Heisenberg the 2nd: adversarially reviewed A5a/A6a at 10:13Z and returned NO-GO until three guardrails were patched: global approval rules must require exact listed phrases, A5a checklist completion must require full candidate evidence plus no denied operations, and A6a checklist preflight must pin the exact stat tuple. After the patch, Heisenberg returned GO/OK.
- Confucius the 2nd: independently verified A1 read-only at 10:23Z and returned GO-TO-ASK only; exact private target refs remain absent, pinned local SHAs still match, private Git transport works, and current public `origin/main` still has zero `ios-native` paths.
- Anscombe the 2nd: adversarially reviewed A1 at 10:23Z and returned NO-GO until the public-origin guard checked both exact A1 target names. After the patch, A1 public-boundary checks require public `origin` to lack both `refs/heads/codex/archive-ios-native-20260614` and `refs/heads/ios-native-app`, and Anscombe returned GO/OK.
- Tesla the 2nd: independently verified A2a/A3a read-only at 10:39Z and returned ASK-ONLY for both preservation packets; A2a dirty-set hash, release metadata, and source/release visibility still match, and A3a installed/cache byte identity plus documented dev-vs-installed three-file drift still match.
- Boole the 2nd: adversarially reviewed A2a/A3a at 10:39Z and returned GO to ask for preservation approvals, with guardrail hardening nits. The docs now use stricter A2a completion evidence, clearer A3a dirty-set authority, fuller local denial lists, and non-deleting `rsync -a` snapshot copies.
- Aquinas the 2nd: independently verified A1 read-only at 10:43Z and returned GO-TO-ASK only; pinned SHAs match, private target refs remain absent, the historical private ref remains present, public `origin` has neither exact A1 target ref, and current public `origin/main` tree remains `truncated=false` with zero `ios-native` paths.
- Helmholtz the 2nd: adversarially reviewed A1 at 10:43Z and returned NO-GO until a public-origin guard bug was patched. The checklist had checked the archive branch name against the archive checkout's `origin`, which points to Oscar; it now checks both exact public refs from `/Users/afar/dev/fieldtheory`, and the A1 runbook template now enforces both public-ref guards.
- Gauss the 2nd: independently verified A5a/A6a read-only at 10:53Z and returned ASK-ONLY for both packets; A5a directory/worktree/tmp/Oscar/mirror/protected-archive evidence still matches, and A6a absent-debris plus exact target-file tuple still matches.
- Hilbert the 2nd: adversarially reviewed A5a/A6a at 10:53Z and returned NO-GO for A5a until exact stop gates were tightened. A5a now pins per-repo worktree counts, exact `/private/tmp` path list, protected archive presence, and Oscar live/mirror compare tuple; A6a was already GO after fresh same-shell preflight.
- Wegener the 2nd: independently verified the 11:15Z A1 local/public facts but hit the known subagent-environment private Git credential-helper failure. The coordinator shell reran the exact private `git ls-remote` checks successfully, so this is recorded as a subagent-environment caution, not coordinator-shell drift. Private Git auth failure in the execution shell remains a stop condition.
- Arendt the 2nd: adversarially reviewed A1 at 11:15Z and returned GO to keep A1 as the current recommended approval ask only; A1 remains request-only, not approved, not executed, and not complete.
- Halley the 2nd: independently verified A2a/A3a read-only at 11:22Z and returned ASK-ONLY for both packets; A2a dirty-set hash, release metadata, source/release visibility, and A3a installed/cache identity plus documented dev-vs-installed drift still match.
- Euclid the 2nd: independently verified A5a/A6a read-only at 11:22Z and returned ASK-ONLY for both packets; A5a directory/worktree/tmp/Oscar/mirror/protected-archive evidence still matches, and A6a absent-debris plus exact target-file tuple still matches.
- Singer the 2nd: adversarially reviewed the 11:22Z non-A1 queue and returned GO to report A2a/A3a/A5a/A6a as limited askable packets only, with A1 still recommended first and A4 still paused.
- Hypatia the 2nd: independently verified the 11:36Z A1 local/public facts but hit the known subagent-environment private Git credential-helper failure. The coordinator shell reran the exact private `git ls-remote` checks successfully, so this is recorded as a subagent-environment caution, not coordinator-shell drift.
- Hooke the 2nd: adversarially reviewed A1 at 11:36Z and returned NO-GO until the post-push upstream-tracking verification was hardened. The A1 packet now runs verification under `set -euo pipefail` and fetches and verifies the two private `refs/remotes/labs/...` refs before setting upstream tracking; Hooke's final rereview returned GO.
- Peirce the 2nd: independently reviewed the 11:59Z source-plan/current-ledger state and live read-only A1 facts. Peirce found A1 sequencing aligned and A1 consistently ask-only, but flagged two older historical A4 wording leaks; those lines now preserve the A4 user pause.
- Nietzsche the 2nd: adversarially reviewed the 11:59Z goal map, approval queue, and A1/A4 gates. Nietzsche returned NO-GO until loose A1 build-waiver and A4 scheduling-waiver wording was converted to exact phrases and stale named subagent assignments were made reusable; after the patch, Nietzsche rereviewed read-only and returned GO.
- Linnaeus the 2nd: independently verified A2a/A3a read-only at 12:14Z. A2a was DRIFTED because the mac app dirty set now has 37 paths and SHA-256 `1f7a98cd04fdde97c48493fc7060843ba6dce13acc0779b3c3805020fa0c135d`; A3a still matches the installed/cache snapshot assumptions.
- Curie the 2nd: independently verified A5a/A6a read-only at 12:14Z. A5a was DRIFTED because `fieldtheory-oss` worktree records now total 12 and four-repo worktree records now total 60; A6a still matches the exact optional quarantine tuple.
- Poincare the 2nd: adversarially reviewed the 12:14Z non-A1 queue and returned NO-GO until loose waiver wording was tightened. Poincare found the queue otherwise stays ask-only, limited to preservation/manifest/quarantine, keeps A1 first, and keeps A4 paused.
- Sagan the 2nd: independently verified A1 read-only at 12:30Z and returned ASKABLE with no drift. Exact private target refs remain absent, pinned local SHAs still match, public target refs remain absent, current public `origin/main` reports `truncated=false` with zero `ios-native` paths, and private Git auth did not fail in that shell.
- Harvey the 2nd: adversarially reviewed A1 at 12:30Z and returned NO-GO until the Phase 1 remaining-gap acceptance path was made exact and the A1 private verification template forced `GIT_TERMINAL_PROMPT=0` on private `ls-remote`, `fetch`, and `git clone`.
- Lovelace the 2nd: independently verified A1/A2a/A3a/A5a/A6a read-only at 12:48Z while A4 remained paused. A1, A3a, A5a, and A6a stayed askable with no material drift; A2a drifted because the mac app dirty set now has 39 paths, 11 Git-visible untracked paths, and SHA-256 `0c9a09a13ed2583ea26f44dffeef44ae42990bdda929265f4f7501696cc4112d`.
- Ohm the 2nd: adversarially reviewed the 12:48Z queue and returned NO-GO until two generic preflight sentences were hardened so user-paused A4 cannot be preflighted or asked about before Andrew explicitly resumes A4 after connecting the backup disk.
- Descartes the 2nd: independently verified A1 read-only at 13:07Z and returned BLOCKED from the subagent shell because private `labs` Git transport hit the known `credential-osxkeychain` failure. Descartes' local/public facts still matched; the coordinator shell reran the exact private `git ls-remote` checks successfully at 13:05Z, so this is a subagent-environment caution, not coordinator-shell drift.
- Huygens the 2nd: adversarially reviewed A1 at 13:05Z and returned GO for the docs/request boundary only. Huygens found no A1 execution authorization, no A4 pause leak, and no A1/Phase 0/Phase 1 completion overclaim.
- Dalton the 2nd: independently verified A1 read-only at 13:22Z and returned ASKABLE from local/public/GitHub API facts, while reproducing the known subagent-only private Git credential-helper failure. The coordinator shell's private Git checks succeeded with `GIT_TERMINAL_PROMPT=0`, so this is a subagent-environment caution, not coordinator-shell drift.
- Carson the 2nd: adversarially reviewed A1 at 13:22Z and returned GO for A1 as the next request-only approval ask, and NO-GO to execute A1, touch A4, or start Phase 1.

## Phase 0 Status

| Unit | State | Current evidence | Next gate |
| --- | --- | --- | --- |
| A1: private iOS preservation | Askable for SHA-pinned approval after upstream-tracking, exact-waiver, and noninteractive private-verification hardening; not executed | `fieldtheory` and `fieldtheory-labs-ios-native-archive` are clean; `afar1/fieldtheory-labs` is private; July 2 13:22Z coordinator-shell exact-ref preflight confirms the two required private branch names are still absent, the older private archive ref remains distinct, public `origin` from `/Users/afar/dev/fieldtheory` has neither exact A1 target ref, current public `origin/main` tree `4df630b96d2c2ec30d03e353dc1364994587457f` reports `truncated=false` and zero `ios-native` paths, and local branch heads still equal `56fab3bf8192826fa9558392927b27cada070af1` and `60e7e9ddbc2f98a0233d8769539b1fb4b189214a`; Dalton the 2nd reproduced the known subagent-only private Git auth failure, while the coordinator shell's private Git checks succeeded; Carson the 2nd found no A1 boundary blocker; local upstream tracking is not final until approved verification | `approve A1 private push and verification 60e7e9ddbc2f98a0233d8769539b1fb4b189214a 56fab3bf8192826fa9558392927b27cada070af1`; complete only after private refs resolve to those SHAs, private remote-tracking refs are fetched and verified non-interactively, upstream tracking is set, fresh private clone contains `ios-native/`, and successful build. If the fresh private clone does not build, A1 remains partial or blocked unless Andrew says `waive A1 fresh-clone build gate after recorded blocker` |
| A2: mac app release train | Not ready to publish; eligible to ask Andrew for A2a approval after 12:48Z dirty-set regeneration, not approved yet | July 2 12:48Z read-only refresh confirms public repo, the release branch is 30 commits ahead of `origin/main`, 31 ahead of upstream release branch, 39 dirty paths, 28 modified tracked paths, 11 Git-visible untracked paths, package `0.3.20`, release feed `0.3.14`, stale changelog/checklist, no source-repo tags, and dirty-set hash `0c9a09a13ed2583ea26f44dffeef44ae42990bdda929265f4f7501696cc4112d`; Lovelace found the only material drift was the two added Git-visible untracked docs plans `docs/plans/2026-07-02-001-feat-ios-live-dictation-plan.md` and `docs/plans/2026-07-02-002-feat-team-share-links-plan.md` | ask for A2a only as bundle/snapshot preservation with phrase `approve A2a create mac-app preservation bundle and dirty-tree snapshot`; commit prep/tag creation/public review/final push require later named approval packets |
| A3: plugin deployed state | Not ready to install or tag; eligible to ask Andrew for A3a approval if dev drift is named, not approved yet | July 2 12:48Z direct read-only preflight confirms installed source and Codex cache are byte-identical but not durably preserved; dev repo is on `main...origin/main`, dirty at `4d7b20ae50659743802a28528a09c1fd1501a799` with status hash `39c3d661ab278fa7c597f092f3f850738334315e78ce8d7f507435838158dc76`, differs from installed source in three files, and has uncommitted follow-on fixes, including untracked CI and sibling skill files; `/private/tmp/a3_dev_files.txt` and `/private/tmp/a3_installed_files.txt` still exist and deletion is not approved | snapshot installed/cache state first; later commit, rename, install, cache-mutation, scratch-file cleanup, or deletion work needs its own named approval packet |
| A4: waist backup | Not complete; user-paused; A4a approval preflight currently fails; A4b blocked | July 2 09:48Z direct preflight plus Pascal the 2nd confirms `restic` missing; `/Volumes/Extreme SSD` absent, not mounted, not writable, and not known to `diskutil info`; Time Machine knows an `Extreme SSD` destination but it is not mounted for A4; no restore-drill artifact; no Field Theory restic/backup scheduler evidence; bookmarks live outside `~/.fieldtheory` through a symlink and must be covered as `~/.ft-bookmarks`; Andrew paused backup work until he connects a backup disk and explicitly resumes A4 | wait for Andrew to explicitly resume A4 after connecting the disk; only then run read-only mount/restic preflight before asking for `approve A4a manual backup and restore drill`; then A4b; complete only after scheduled job exists and scheduled-snapshot restore passes, unless Andrew says `waive A4 scheduled-backup gate after manual backup and restore drill` |
| A5: constellation cleanup | Not cleanup-ready; eligible to ask Andrew for A5a manifest-only approval after 12:14Z worktree-count regeneration and 12:48Z no-drift recheck, not approved yet | July 2 12:48Z direct read-only preflight found 59 Field Theory-family dirs, 60 target-repo worktree records, 5 `/private/tmp` candidates, 17 prunable `fieldtheory-cli` worktree records, stale `oscar` mirror `1454e11c9b0b47b30b4ef0b356f334d2d6b8bb28` 12 commits behind live GitHub `origin/main` `f81481ff9824ddb4fa252ed93933d9bc815dc930`, protected iOS archive still present, and dirty/preserved candidates needing target-specific preservation decisions; the per-repo worktree tuple remains `fieldtheory-oss=12`, `fieldtheory=17`, `fieldtheory-cli=30`, `fieldtheory-plugin=1` | ask for A5a only as manifest work with no archives, bundles, waivers, deletion, prune, or move |
| A6: review debris | Only optional quarantine remains; eligible to ask Andrew for approval, not approved yet | July 2 12:48Z direct read-only preflight confirms accidental `bookmarks.db?immutable=1` is gone; zero-byte regular `twitter-bookmarks.db` still exists, is not a symlink, and has exact tuple `70248019 0 1776019722 Regular File` with modified time `Apr 12 14:48:42 2026` | optional guarded quarantine only, not direct `rm` |

Phase 0 exit is not met. It requires at least: iOS on private remote with fetched private remote-tracking refs, upstream/fresh-clone/build proof, mac app main pushed/tagged, scheduled backup installed and observed with restore drill passed, and `~/dev` family reduced after safe preservation.

## Completed Orchestration Work

- Created `docs/PRESERVATION_GATES.md` with A1-A6 current evidence and approval-only operation shapes.
- Created `docs/PHASE0_APPROVAL_PACKETS.md` with named approval scopes and completion evidence for the next safe Phase 0 actions.
- Created `docs/PHASE0_PREFLIGHT_CHECKLIST.md` with read-only checks that must still match packet assumptions before approval is used.
- Created `docs/PHASE0_PREFLIGHT_RESULTS.md` and recorded the July 2 read-only A1 evidence refresh.
- Re-ran the July 2 03:00Z read-only A1 evidence refresh with an independent subagent; A1 remains askable but not approved or complete.
- Re-ran the July 2 03:35Z read-only A1 evidence refresh with independent evidence and adversarial subagents; A1 remains askable but not approved or complete.
- Re-ran the July 2 04:15Z A1 goal-contract review with Copernicus and Franklin. Direct private Git transport succeeded in the orchestrator shell, but Franklin found a valid gate bug: A1 approval must bind to exact SHAs, not branch names only. The A1 packet is now SHA-pinned.
- Re-ran and recorded the July 2 04:27Z post-patch A1 SHA-pinned preflight in `docs/PHASE0_PREFLIGHT_RESULTS.md`. A1 remains ready to ask with the SHA-pinned phrase, not approved, and not complete.
- Re-ran and recorded the July 2 04:52Z A1 SHA-pinned preflight. The public `origin/main` remote advanced beyond local `origin/main`, so the public-leak check now uses GitHub's tree API for the current remote SHA; A1 remains ready to ask, not approved, and not complete.
- Re-ran and recorded the July 2 05:15Z A1 SHA-pinned preflight. A1 has no material drift: exact private refs are still absent, local branch heads still match the approval SHAs, and current public `origin/main` still has zero `ios-native` paths.
- Ran a source-plan coverage audit with Hypatia. The ledger covers A1-A6, A1/A4 gating, dependency ordering, and Phase 0 exit criteria; no material source-plan drift was found.
- Reran Singer's adversarial review after the evidence-log patch; no blockers remain on presenting only the SHA-pinned A1 approval gate.
- Recorded the July 2 read-only A4a preflight failure: approved backup volume missing, `restic` missing, no restore-drill artifacts.
- Re-ran the July 2 03:10Z read-only A4 evidence refresh with an independent subagent; A4a remains not askable and A4b remains blocked.
- Re-ran the July 2 03:44Z read-only A4 evidence refresh with an independent subagent; A4a remains not askable, A4b remains blocked, and Time Machine's `Extreme SSD` destination is not mounted for A4.
- Re-ran the July 2 04:58Z read-only A4 evidence refresh. A4a remains not askable, A4b remains blocked, `restic` is still missing, and the backup-looking local folders still do not prove restore safety.
- Re-ran the July 2 05:15Z read-only A4 evidence refresh. A4a remains not askable, A4b remains blocked, `/Volumes/Extreme SSD` is still absent, `restic` is still missing, and no restore-drill artifact exists.
- Recorded the July 2 read-only A2a/A3a evidence refreshes: A2a and A3a are eligible to ask Andrew for approval, but not approved yet; neither act nor sub-packet is complete.
- Re-ran the July 2 03:16Z read-only A2 evidence refresh with an independent subagent; A2a remains askable as preservation only, and A2 remains incomplete.
- Re-ran the July 2 03:25Z read-only A3 evidence refresh with an independent subagent; A3a remains askable as installed/cache preservation only, and A3 remains incomplete.
- Re-ran the July 2 04:31Z read-only A2/A3 evidence refreshes with independent subagents. A2 has no material drift. A3 has one wording correction: sibling `agents/openai.yaml` files are present in installed/cache state, but the dev repo still has untracked follow-on sibling skill files. A3a remains installed/cache snapshot only.
- Re-ran the July 2 05:05Z/05:06Z read-only A2/A3 evidence refreshes. A2 still has no material drift, and A3 still has installed/cache byte-identity with dev-bundle drift in the same three files; the accidental `/private/tmp/a3_dev_files.txt` and `/private/tmp/a3_installed_files.txt` scratch files still exist and remain deletion-gated.
- Recorded the July 2 read-only A5/A6 evidence refreshes: A5a is eligible to ask Andrew for manifest-only approval, but not approved yet; A6 optional quarantine is eligible to ask Andrew for approval, but not approved yet.
- Re-ran the July 2 03:56Z read-only A5/A6 evidence refresh with an independent subagent; A5 deletion/prune/move remains blocked, A5a remains askable as manifest/evidence only, and A6 remains askable as optional quarantine only.
- Re-ran the July 2 04:40Z read-only A5/A6 evidence refresh with independent subagents. A5/A6 have no material drift; A5's stale `oscar` mirror is now quantified as 6 commits behind local `oscar` and 10 behind `origin/main`.
- Re-ran the July 2 05:24Z/05:29Z read-only A5/A6 evidence refresh. A5 remains manifest-only, but Darwin found one live Oscar remote drift correction: local `origin/main` was stale, and the mirror is 12 commits behind live GitHub `origin/main`. A6 remains optional quarantine-only.
- Re-ran the July 2 05:46Z/05:49Z Phase 0 refresh with explicit subagent goals. A1 remains askable with the SHA-pinned phrase, A4 remains blocked, Galileo found no A2/A3/A5/A6 drift, and Harvey found no blocker to A1-first sequencing.
- Re-ran the July 2 06:10Z read-only A1 approval-request refresh with explicit coordinator, evidence-subagent, and adversarial-subagent goals. A1 remains askable with the SHA-pinned phrase, not approved, and not complete. `docs/A1_APPROVAL_REQUEST.md` is request text only.
- Patched Maxwell's adversarial boundary findings: Workstream A in the source plan is now goals-only, mutating approval-only blocks in `docs/PRESERVATION_GATES.md` are text templates, A4 volume proof now precedes any restic install, and subagent-only A2/A3/A5/A6 findings are labeled as leads requiring direct preflight before asking approval.
- Reran Maxwell's adversarial boundary review after the patches. Maxwell returned GO to ask Andrew with the A1 request text only, and explicitly not GO to execute anything.
- Re-ran the July 2 06:21Z direct A2a/A3a/A5a/A6a evidence refresh with Franklin and Locke as independent read-only evidence subagents. A2a, A3a, A5a, and A6a remain askable only as their named limited packets; none are approved, executed, or complete.
- Patched Einstein's adversarial non-A1 request-queue findings: the source plan now frames A2 as A2a preservation-first, A5 as manifest-only, and A6 as optional quarantine-only; `docs/PRESERVATION_GATES.md` now anchors A2/A3/A5/A6 to the 06:21Z direct preflight; the A5 mirror evidence now plainly says live `origin/main` is 12 commits ahead of the mirror. Einstein rereviewed and returned GO for the changed areas.
- Re-ran the July 2 07:42-07:44Z goal-scoped A2a/A3a/A5a/A6a refresh after assigning explicit coordinator and subagent goals. Dirac independently confirmed A2a/A3a remain ASK-ONLY with no material drift; Cicero independently confirmed A5a/A6a remain ASK-ONLY with no material drift. No approval packet was executed.
- Re-ran the July 2 06:37Z direct A4 backup-gate evidence refresh with Parfit and Ptolemy as independent read-only evidence subagents. A4a remains not askable, A4b remains blocked, `/Volumes/Extreme SSD` is still absent, `restic` is still missing, no scheduler evidence exists, and no restore-drill artifact exists.
- Patched Aristotle's adversarial A4 wording finding: the A4 setup, backup, and restore-drill guards now require a fresh A4a preflight that PASSES with the approved volume mounted, writable, and verified by mount/disk metadata. Hume rereviewed and returned GO.
- Re-ran the July 2 06:49Z direct A1 approval-request preflight with Laplace as an independent read-only evidence subagent. A1 remains eligible to ask with the SHA-pinned phrase, not approved, and not complete. Laplace's private Git credential-helper failure did not reproduce in the coordinator shell, where exact private `git ls-remote` checks succeeded; private Git auth failure in the execution shell remains a stop condition.
- Ran McClintock's adversarial review on the 06:49Z A1 evidence refresh. McClintock found no blockers and confirmed A1 remains request-only until Andrew says the exact phrase and the preflight still matches.
- Re-ran the July 2 07:00Z direct A1 pre-approval check and Euler's adversarial sequencing review. A1 remains the recommended next ask; A4 remains blocked, and A2a/A3a/A5a/A6a first would be less aligned with the source-plan risk ordering.
- Discovered and recorded the README-backed A1 post-push build verification path with Mendel and Chandrasekhar. The A1 packet now names `./ios-native/scripts/build-whisper-ios.sh`, `xcodegen generate`, and the non-signing simulator/no-code-sign `xcodebuild -project FieldTheory.xcodeproj -scheme FieldTheory ... build` command, plus the generated-artifact caveats.
- Re-ran the July 2 07:57-08:00Z direct A1/A4 gate refresh with Fermat and Nash as independent read-only evidence subagents and Averroes as the adversarial sequencing reviewer. A1 remains the recommended next approval ask, not executed and not complete; A4a approval remains not askable until the approved volume is mounted and verified, and A4b remains blocked. Averroes returned GO for the sequencing boundary.
- Used read-only subagents for A2, A3, A5/A6 evidence.
- Used adversarial document review on the preservation runbook until no blockers remained.
- C1 CLI JSON contract implementation is present and currently verifies locally: focused JSON/bookmark tests passed, `npm run build` passed, `git diff --check` passed, and `npm test` passed 590 tests. It is still WIP for sequencing purposes because it is uncommitted and Phase 1 remains gated by A1/A4. If any Phase 0 exit gap remains after A1 and A4 pass, Andrew must say the exact phrase `accept Phase 1 start with remaining Phase 0 gaps: <named gap IDs>` before Phase 1 starts.
- Created `docs/ECOSYSTEM_ACT_GOALS.md` as the coordinator/subagent goal map for Workstreams A-G. Leibniz adversarially reviewed the need for this map and returned GO, with the guard that the file remains coordination-only and cannot imply approval or completion.
- Added the July 2 08:15Z current-session goal assignments to `docs/ECOSYSTEM_ACT_GOALS.md`. Volta the 2nd returned GO on the goal map and approval queue. Beauvoir the 2nd returned NO-GO on three safety blockers, which were patched: A1 pre-existing target refs now stop/regenerate instead of continuing, A1 build verification is non-signing simulator/no-code-sign, and the A4 restore drill uses a unique temp directory instead of deleting a fixed restore path.
- Re-ran the July 2 08:20Z direct read-only A1 pre-approval check with Bohr the 2nd as independent evidence subagent and Lagrange the 2nd as adversarial reviewer. A1 remains GO-to-ask only, not approved, not executed, and not complete. Lagrange reported an accidental no-op default `git push` from `fieldtheory-cli`; the coordinator verified HEAD, upstream, and remote branch all remained at `c9a14820d537c1927a149d13d7211ea3727e3dbd`.
- Re-ran the July 2 08:27Z direct read-only A2a preflight with Schrodinger the 2nd as independent evidence subagent and Herschel the 2nd as adversarial reviewer. A2a remains ASK-ONLY, not approved, not executed, and not complete. Created `docs/A2A_APPROVAL_REQUEST.md`; hardened the A2a bundle template to refuse reusing an existing snapshot folder or bundle and clarified that ignored files are not included in the Git-visible untracked archive.
- Re-ran the July 2 08:38-08:42Z direct read-only A3a preflight with Darwin the 2nd as independent evidence subagent and Archimedes the 2nd as adversarial reviewer. A3a remains ASK-ONLY, not approved, not executed, and not complete. Created `docs/A3A_APPROVAL_REQUEST.md`; hardened the A3a installed/cache snapshot template to refuse reusing an existing snapshot directory and to avoid self-including `SHA256SUMS`.
- Re-ran the July 2 08:48-08:50Z direct read-only A5a preflight with James the 2nd as independent evidence subagent and Avicenna the 2nd as adversarial reviewer. A5a remains ASK-ONLY, not approved, not executed, and not complete. Created `docs/A5A_APPROVAL_REQUEST.md`; hardened the A5a manifest template so it records every fresh-preflight candidate, live Oscar GitHub evidence, protected iOS archive status, and future preservation decision paths without approving cleanup.
- Re-ran the July 2 08:56-08:59Z direct read-only A6a preflight with Feynman the 2nd as independent evidence subagent and Planck the 2nd as adversarial reviewer. A6a remains ASK-ONLY, not approved, not executed, and not complete. Created `docs/A6A_APPROVAL_REQUEST.md`; hardened the A6a quarantine template to require a fresh quarantine directory, destination overwrite guard, immediate pre-move checks, and post-move proof.
- Reconciled `docs/PHASE0_COMPLETION_AUDIT.md` at 09:03Z so it now includes the A3a, A5a, and A6a request artifacts while preserving that A1 remains the recommended next packet, A4 remains blocked, and Phase 1 remains gated.
- Re-ran the July 2 09:42Z direct read-only A1 preflight with Faraday the 2nd as independent evidence subagent and Kepler the 2nd as adversarial reviewer. A1 remains GO-to-ask-only with the SHA-pinned phrase, not approved, not executed, and not complete.
- Re-ran the July 2 09:48Z direct read-only A4 preflight with Pascal the 2nd as independent evidence subagent and Goodall the 2nd as adversarial reviewer. A4a remains not askable, A4b remains blocked, and the A4 backup-scope wording now explicitly treats `~/.fieldtheory` plus `~/.ft-bookmarks` as required roots.
- Re-ran the July 2 09:56Z direct read-only A2a/A3a preflight with Raman the 2nd as independent evidence subagent and Turing the 2nd as adversarial reviewer. A2a remains ASK-ONLY with no meaningful drift. A3a remains an installed/cache preservation ask only, and Turing returned OK after the request was patched to pin plugin dev-repo upstream/status hash and stop if dev-repo evidence changes.
- Re-ran the July 2 10:13Z direct read-only A5a/A6a preflight with Sartre the 2nd as independent evidence subagent and Heisenberg the 2nd as adversarial reviewer. A5a and A6a remain ASK-ONLY with no evidence drift. Heisenberg's guardrail blockers were patched: exact approval phrases are required globally, A5a checklist completion now requires full candidate coverage plus no denied operations, and A6a checklist preflight now pins the exact target stat tuple. Heisenberg rereviewed the patch and returned GO/OK.
- Re-ran the July 2 10:23Z direct read-only A1 preflight with Confucius the 2nd as independent evidence subagent and Anscombe the 2nd as adversarial reviewer. A1 remains GO-TO-ASK only with no evidence drift. Anscombe found one guardrail blocker: public-origin exact-ref checks covered `ios-native-app` but not `codex/archive-ios-native-20260614`; the A1 checklist and request now check both exact public target names. Anscombe rereviewed the patch and returned GO/OK.
- Ran the July 2 11:10Z source-plan coverage and adversarial queue review with Copernicus the 2nd and Dewey the 2nd. Copernicus returned GO for keeping A1 as the next approval ask and noted later dependency edges to record; Dewey returned NO-GO until A4 pause language stopped nudging disk-connection prep. The docs now mark A4 user-paused until Andrew explicitly resumes it after connecting a backup disk.
- Re-ran the July 2 11:15Z direct read-only A1 preflight with Wegener the 2nd as independent evidence subagent and Arendt the 2nd as adversarial reviewer. A1 remains GO-to-ask-only in the coordinator shell, not approved, not executed, and not complete. Wegener's private Git credential-helper failure did not reproduce in the coordinator shell; Arendt found no A1 queue blocker.
- Re-ran the July 2 11:22Z direct read-only A2a/A3a/A5a/A6a preflight with Halley the 2nd and Euclid the 2nd as independent evidence subagents and Singer the 2nd as adversarial reviewer. A2a, A3a, A5a, and A6a remain ASK-ONLY, not approved, not executed, and not complete; A1 remains the recommended first ask, and A4 remains user-paused.
- Re-ran the July 2 11:36Z direct read-only A1 preflight with Hypatia the 2nd as independent evidence subagent and Hooke the 2nd as adversarial reviewer. A1 has no coordinator-shell evidence drift and remains request-only, not approved, not executed, and not complete. Hypatia's private Git credential-helper failure did not reproduce in the coordinator shell. Hooke found real guardrail issues, now patched: the post-push verification runs under `set -euo pipefail` and fetches and verifies private remote-tracking refs before setting upstreams. Hooke's final rereview returned GO.
- Re-ran the July 2 11:59Z direct read-only A1/orchestration-boundary preflight with Peirce the 2nd as independent evidence subagent and Nietzsche the 2nd as adversarial reviewer. A1 has no coordinator-shell evidence drift and remains request-only, not approved, not executed, and not complete. Peirce's historical A4 pause-wording leak was patched. Nietzsche's exact-waiver blockers were patched: A1 build waiver now requires `waive A1 fresh-clone build gate after recorded blocker`, A4 scheduling waiver now requires `waive A4 scheduled-backup gate after manual backup and restore drill`, and stale named subagent assignments are now reusable goal slots. Nietzsche rereviewed the patch and returned GO.
- Re-ran the July 2 12:14Z direct read-only A2a/A3a/A5a/A6a preflight with Linnaeus the 2nd and Curie the 2nd as independent evidence subagents and Poincare the 2nd as adversarial reviewer. A2a and A5a drifted from their pinned tuples and were regenerated; A3a and A6a still match. Poincare found no queue overclaim but required three loose waiver lines to be tightened.
- Re-ran the July 2 12:48Z direct read-only A1/A2a/A3a/A5a/A6a preflight with Lovelace the 2nd as independent evidence subagent and Ohm the 2nd as adversarial reviewer. A1, A3a, A5a, and A6a remain ASKABLE with no material drift; A2a drifted and was regenerated to the 39-path dirty set with hash `0c9a09a13ed2583ea26f44dffeef44ae42990bdda929265f4f7501696cc4112d`; A4 stayed user-paused and no A4 preflights were run. Ohm's A4 pause wording blockers were patched.
- Re-ran the July 2 13:05Z direct read-only A1 preflight with Descartes the 2nd as independent evidence subagent and Huygens the 2nd as adversarial reviewer. A1 remains askable in the coordinator shell, not approved, not executed, and not complete; Descartes hit the known subagent private Git auth failure; Huygens found no documentation blocker. A4 stayed user-paused and no A4 preflights were run.
- Re-ran the July 2 13:22Z direct read-only A1 preflight with Dalton the 2nd as independent evidence subagent and Carson the 2nd as adversarial reviewer. A1 remains askable in the coordinator shell, not approved, not executed, and not complete; Dalton hit the known subagent private Git auth failure; Carson found no blocker and recommended request-only wording hardening, now applied. A4 stayed user-paused and no A4 preflights were run.

## Do Not Start Yet

These items are plan-valid but blocked by preservation gates:

- Phase 1 work as an unblocker, including B1/B2/B5/C1/C4/C5/D2/D4: blocked until A1 and A4 pass. If any Phase 0 exit gap remains after A1 and A4 pass, Andrew must say the exact phrase `accept Phase 1 start with remaining Phase 0 gaps: <named gap IDs>` before Phase 1 starts. A1 build and A4 scheduling have their own exact waiver phrases. Local C1 verification does not unlock C2/C3 by itself.
- `docs/DATA_OWNERSHIP.md` is B2 documentation evidence only; it does not enforce the ownership contract or unblock B2 enforcement.
- E1/E2 iOS extraction/distribution: blocked by A1.
- B3/B4/F1 waist and bookmark data migrations: blocked by A4.
- G3 pocket capture suite depends on E3 capture surfaces.
- G5 command marketplace depends on F4 commands/ideas sync.
- G6 self-hostable cloud depends on D6 shipped Supabase schema and RLS evidence.
- A5 deletion/prune steps: blocked by per-target preservation and explicit deletion approval.
- Public mac app release push/publish: blocked by A2 working-tree preservation, clean release prep, approved exposure-scan disposition, green release verification evidence, and Andrew's exact approval phrase.
- Plugin cache install: blocked by A3a installed/cache snapshot completion, later commit/rename readiness, and a separate named install/cache-mutation packet with an exact approval phrase.

## Current Approval Queue

These packets are askable now, but none are approved. Do not execute any packet unless Andrew says the exact approval phrase from `docs/PHASE0_APPROVAL_PACKETS.md` and the matching read-only preflight is rerun in the same execution shell and still matches.

Recommended next packet:

- `approve A1 private push and verification 60e7e9ddbc2f98a0233d8769539b1fb4b189214a 56fab3bf8192826fa9558392927b27cada070af1` — highest value because A1 gates later iOS extraction and distribution. It remains incomplete until both private branches exist at those exact SHAs, both fetched private remote-tracking refs resolve to those SHAs, both local branches track the private upstream, a fresh private clone contains `ios-native/`, and the fresh clone builds. If the fresh private clone does not build, A1 remains partial or blocked unless Andrew says `waive A1 fresh-clone build gate after recorded blocker`.

Other askable preservation packets:

- `approve A2a create mac-app preservation bundle and dirty-tree snapshot` — preserves the dirty public mac app release train only against the regenerated 12:48Z dirty-set tuple: 39 dirty paths, 28 modified tracked paths, 11 Git-visible untracked paths, SHA-256 `0c9a09a13ed2583ea26f44dffeef44ae42990bdda929265f4f7501696cc4112d`, including `docs/plans/2026-07-02-001-feat-ios-live-dictation-plan.md` and `docs/plans/2026-07-02-002-feat-team-share-links-plan.md`. It does not approve stash, commit, branch switch, tag, push, release publication, or A2 completion.
- `approve A3a plugin installed-state snapshot` — snapshots currently installed plugin/cache state only. It does not approve install, cache mutation, dev repo commit, directory rename, `install.sh`, `/private/tmp/a3_dev_files.txt` or `/private/tmp/a3_installed_files.txt` deletion, pushes, migrations, backup/restore work, or A3 completion.
- `approve A5a cleanup manifest only` — creates a cleanup manifest only against the regenerated 12:14Z worktree-count tuple, rechecked at 12:48Z with no material drift. It does not approve archives, bundles, deletion, `git worktree prune`, moving `oscar`, creating waivers, clone/build/install work, pushes, tags, migrations, backup/restore work, plugin-cache mutation, or A5 completion.
- `approve A6a review-debris quarantine` — optionally quarantines the exact zero-byte `twitter-bookmarks.db` only. It does not approve deletion, touching other bookmark databases or media files, symlink changes, bookmark migrations, broad cleanup, A5 completion, Phase 0 completion, backup/restore work, clone/build/install work, manifests, bundles, archives, snapshots, plugin-cache mutation, tags, scheduled jobs, worktree pruning, stash, commit, push, or any other move.

Currently blocked packets:

- `approve A4a manual backup and restore drill` — user-paused and not askable until Andrew explicitly resumes A4 after connecting the disk; only then may `/Volumes/Extreme SSD` be verified as mounted/writable by mount/disk metadata. `restic` is also missing and must be named before asking.
- `approve A4b scheduled backup and restore drill` — blocked until A4a passes and the exact schedule/log/repository/restore-drill shape is reviewed.
- `approve A2b public release review only` — blocked until A2a preservation is complete and the release train is clean and internally consistent.
- `approve A2c final public release push <reviewed-sha> v0.3.20` — blocked until A2a and A2b are complete, release verification is green, the reviewed SHA is known, and the exact tag/ref state is prepared.

Phase 0 is not complete after any single askable packet. The exit condition still requires private iOS preservation with build proof, mac app source/tag/release evidence, scheduled waist backup with restore proof, and safe `~/dev` cleanup after preservation.

## Active Subagent Pattern

For each act:

1. One evidence agent inspects current state read-only.
2. One adversarial reviewer tries to break the proposed operation.
3. The orchestrator patches the runbook or implementation.
4. The orchestrator verifies current state directly before claiming completion.

Do not let subagent summaries become proof by themselves. They are evidence leads; command output and current files remain authoritative.
